# План: Новый сайт Cosmo Capital

**Дата старта:** 2026-05-21
**Продукт:** cosmacapital.ru — агентство коммерческой недвижимости
**Цель:** Каталог объектов (аренда/продажа) + поиск/фильтры + встроенная мини-CRM

---

## Стек

| Слой | Технология | Обоснование |
|------|-----------|-------------|
| Framework | Next.js 15 (App Router) + TypeScript | уже настроен |
| Styles | Tailwind CSS v4 | уже настроен |
| БД | **Supabase** (PostgreSQL) | хостинг + storage для фото + auth + дашборд |
| ORM | **Prisma** | типизированные запросы, миграции |
| Auth (admin) | **NextAuth.js** + credentials | email/пароль для 1 владельца |
| Хранение фото | **Supabase Storage** | S3-совместимое, бесплатный тир |
| Deploy | **Vercel** | нативная поддержка Next.js |

---

## Схема базы данных

### Таблица `objects` — объекты недвижимости

| Поле | Тип | Описание |
|------|-----|---------|
| id | uuid | PK |
| type | enum(RENT, SALE) | тип сделки |
| category | enum(OFFICE, RETAIL, WAREHOUSE, FREE_PURPOSE, PRODUCTION) | категория |
| title | text | название объекта |
| description | text | полное описание |
| address | text | адрес |
| metro | text? | ближайшее метро |
| area_total | float | общая площадь, кв.м |
| area_min | float? | минимальная секция (для аренды) |
| floor | int? | этаж |
| floors_total | int? | этажей в здании |
| price | float | цена (руб/мес для аренды, руб для продажи) |
| price_per_sqm | float? | цена за кв.м (автовычисляется) |
| photos | text[] | массив URL из Supabase Storage |
| status | enum(ACTIVE, ARCHIVED) | статус публикации |
| featured | bool | показывать в топе |
| created_at | timestamp | |
| updated_at | timestamp | |

### Таблица `leads` — заявки (мини-CRM)

| Поле | Тип | Описание |
|------|-----|---------|
| id | uuid | PK |
| name | text | имя клиента |
| phone | text | телефон |
| email | text? | email |
| message | text? | сообщение |
| object_id | uuid? | FK -> objects (с какого объекта заявка) |
| source | text | "catalog" / "object_page" / "hero" |
| status | enum(NEW, IN_WORK, DONE, REJECTED) | статус обработки |
| notes | text? | заметки менеджера |
| created_at | timestamp | |

### Таблица `services` — услуги компании

| Поле | Тип | Описание |
|------|-----|---------|
| id | uuid | PK |
| title | text | название услуги |
| description | text | описание |
| icon | text? | emoji или имя иконки |
| order | int | порядок отображения |
| active | bool | показывать или нет |

---

## Фазы

### Фаза 1 - Research и планирование `[x]`
- Опрос владельца, определение стека и схемы БД
- Создание скелета проекта (Next.js + Tailwind)

**Deliverable:** этот файл плана + скелет закоммичен

---

### Фаза 2 - Supabase + Prisma `[ ]`
**Цель:** рабочая БД с миграциями и сид-данными

**Задачи:**
- [ ] Создать проект на Supabase (supabase.com)
- [ ] Добавить `.env.local` с `DATABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- [ ] `npm install prisma @prisma/client`
- [ ] `npx prisma init`
- [ ] Написать schema.prisma (objects, leads, services)
- [ ] `npx prisma migrate dev --name init`
- [ ] Создать `prisma/seed.ts` с 5-10 тестовыми объектами
- [ ] Проверить: `npx prisma studio` показывает данные

**Критерий «сделано»:** `npx prisma migrate deploy` без ошибок, seed залит.

---

### Фаза 3 - API Routes (объекты) `[ ]`
**Цель:** REST API для каталога

**Задачи:**
- [ ] `GET /api/objects` — список с фильтрами (type, category, area_min-max, price_min-max, metro)
- [ ] `GET /api/objects/[id]` — детальная карточка
- [ ] `POST /api/leads` — создать заявку
- [ ] Валидация входных данных (zod)
- [ ] Тесты: проверить все эндпоинты через curl/Postman

**Критерий «сделано»:** все 3 роута возвращают корректные данные.

---

### Фаза 4 - Каталог (фронтенд) `[ ]`
**Цель:** страница `/catalog` с поиском и фильтрами

**Задачи:**
- [ ] `src/app/catalog/page.tsx` — серверный компонент, SSR-фильтрация через searchParams
- [ ] Компонент `ObjectCard` (фото, тип, площадь, цена, адрес)
- [ ] Компонент `FilterPanel` (тип сделки, категория, площадь от/до, цена от/до)
- [ ] URL-синхронизация фильтров (`?type=RENT&category=OFFICE`)
- [ ] Пагинация (12 объектов на страницу)
- [ ] Пустое состояние (нет результатов)
- [ ] Loading skeleton

**Критерий «сделано»:** фильтрация работает, URL обновляется, SSR.

---

### Фаза 5 - Страница объекта `[ ]`
**Цель:** `/catalog/[id]` — детальная карточка

**Задачи:**
- [ ] Галерея фото (lightbox)
- [ ] Все характеристики объекта
- [ ] Форма заявки по конкретному объекту (POST /api/leads с object_id)
- [ ] `generateStaticParams` для статической генерации
- [ ] OpenGraph метатеги (для шаринга)

**Критерий «сделано»:** страница рендерится статически, форма сохраняет заявку в БД.

---

### Фаза 6 - Загрузка фото `[ ]`
**Цель:** фото объектов хранятся в Supabase Storage

**Задачи:**
- [ ] Настроить Supabase Storage bucket `objects`
- [ ] `POST /api/upload` — загрузка файла, возвращает URL
- [ ] Ограничение: только jpg/png, до 10 МБ
- [ ] Оптимизация: `next/image` с Supabase домейном в `next.config.ts`

**Критерий «сделано»:** фото загружается через API, отображается через `next/image`.

---

### Фаза 7 - Admin панель (мини-CRM) `[ ]`
**Цель:** `/admin` — управление объектами и заявками

**Задачи:**
- [ ] `npm install next-auth @auth/prisma-adapter`
- [ ] Защита роута `/admin` через NextAuth middleware
- [ ] `/admin/objects` — таблица объектов, кнопки Edit/Archive
- [ ] `/admin/objects/new` — форма создания объекта с загрузкой фото
- [ ] `/admin/objects/[id]/edit` — форма редактирования
- [ ] `/admin/leads` — таблица заявок с фильтром по статусу
- [ ] Смена статуса заявки (NEW -> IN_WORK -> DONE)
- [ ] Поле «заметки» к заявке

**Критерий «сделано»:** можно добавить объект через UI, заявка меняет статус.

---

### Фаза 8 - Главная + страницы `[ ]`
**Цель:** полный сайт с главной, услугами, контактами

**Задачи:**
- [ ] Главная: Hero + Featured объекты (из БД, featured=true) + Услуги + CTA + Контакты
- [ ] Услуги: управляются через таблицу `services` в БД
- [ ] SEO: `generateMetadata` на каждой странице
- [ ] Карта (Яндекс.Карты или статичная картинка) в контактах
- [ ] Юридические страницы: `/privacy`, `/terms` (статичные)

**Критерий «сделано»:** lighthouse score > 90, все страницы рендерятся.

---

### Фаза 9 - Дизайн и полировка `[ ]`
**Цель:** профессиональный вид, адаптив, детали

**Задачи:**
- [ ] Typography: подключить локальные или Google Fonts
- [ ] Цветовая схема: утвердить с владельцем
- [ ] Адаптив: проверить на 320px, 768px, 1440px
- [ ] Анимации: Framer Motion для карточек (опционально)
- [ ] Favicon, OG-изображение
- [ ] 404 страница

---

### Фаза 10 - Deploy `[ ]`
**Цель:** продакшн на Vercel + Supabase

**Задачи:**
- [ ] `vercel --prod` первый деплой
- [ ] Env vars в Vercel Dashboard
- [ ] `npx prisma migrate deploy` на проде
- [ ] Кастомный домен cosmacapital.ru -> Vercel
- [ ] SSL автоматически через Vercel
- [ ] Smoke test: открыть каталог, отправить заявку, зайти в /admin

**Критерий «сделано»:** сайт живет на cosmacapital.ru, заявки приходят в /admin/leads.

---

## Итог (заполнить после завершения)

### Реализовано
- [ ] ...

### Коммиты по фазам
- Фаза 1 → `6b4ebbb` "feat: add full site skeleton"
- Фаза 2 → ...

---

## Открытые вопросы

- Нужны ли уведомления о новых заявках (email/Telegram)? Если да — добавить в Фазу 7.
- Цветовая схема сайта — уточнить в Фазе 9.
- Нужен ли экспорт заявок в Excel?
