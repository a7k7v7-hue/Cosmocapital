# План: Новый сайт Cosmo Capital

**Дата старта:** 2026-05-21
**Продукт:** cosmacapital.ru — агентство коммерческой недвижимости
**Цель:** Каталог объектов (аренда/продажа) + поиск/фильтры + встроенная мини-CRM

---

## Стек

| Слой | Технология | Обоснование |
|------|-----------|-------------|
| Framework | Next.js 15 (App Router) + TypeScript | уже настроен |
| Styles | Tailwind CSS v4 | уже настроен |
| БД | **Supabase** (PostgreSQL) | хостинг + storage для фото + auth + дашборд |
| ORM | **Prisma** | типизированные запросы, миграции |
| Auth (admin) | **NextAuth.js** + credentials | email/пароль для 1 владельца |
| Хранение фото | **Supabase Storage** | S3-совместимое, бесплатный тир |
| Deploy | **Vercel** | нативная поддержка Next.js |

---

## Схема базы данных

### Таблица `objects` — объекты недвижимости

| Поле | Тип | Описание |
|------|-----|---------|
| id | uuid | PK |
| type | enum(RENT, SALE) | тип сделки |
| category | enum(OFFICE, RETAIL, WAREHOUSE, FREE_PURPOSE, PRODUCTION) | категория |
| title | text | название объекта |
| description | text | полное описание |
| address | text | адрес |
| metro | text? | ближайшее метро |
| area_total | float | общая площадь, кв.м |
| area_min | float? | минимальная секция (для аренды) |
| floor | int? | этаж |
| floors_total | int? | этажей в здании |
| price | float | цена (руб/мес для аренды, руб для продажи) |
| price_per_sqm | float? | цена за кв.м (автовычисляется) |
| photos | text[] | массив URL из Supabase Storage |
| status | enum(ACTIVE, ARCHIVED) | статус публикации |
| featured | bool | показывать в топе |
| created_at | timestamp | |
| updated_at | timestamp | |

### Таблица `leads` — заявки (мини-CRM)

| Поле | Тип | Описание |
|------|-----|---------|
| id | uuid | PK |
| name | text | имя клиента |
| phone | text | телефон |
| email | text? | email |
| message | text? | сообщение |
| object_id | uuid? | FK -> objects (с какого объекта заявка) |
| source | text | "catalog" / "object_page" / "hero" |
| status | enum(NEW, IN_WORK, DONE, REJECTED) | статус обработки |
| notes | text? | заметки менеджера |
| created_at | timestamp | |

### Таблица `services` — услуги компании

| Поле | Тип | Описание |
|------|-----|---------|
| id | uuid | PK |
| title | text | название услуги |
| description | text | описание |
| icon | text? | emoji или имя иконки |
| order | int | порядок отображения |
| active | bool | показывать или нет |

---

## Фазы

### Фаза 1 - Research и планирование `[x]`
- Опрос владельца, определение стека и схемы БД
- Создание скелета проекта (Next.js + Tailwind)

**Deliverable:** этот файл плана + скелет закоммичен

---

### Фаза 2 - Supabase + Prisma `[x]` → `4e8f326` "feat(phase-2): Prisma schema"
**Цель:** рабочая БД с миграциями и сид-данными

**Задачи:**
- [x] `npm install prisma @prisma/client zod`
- [x] `npx prisma init`
- [x] Написать schema.prisma (Object, Lead, Service + все enum'ы)
- [x] Создать `prisma/seed.ts` с 6 объектами и 6 услугами
- [x] Создать `.env.local.example` шаблон
- [x] `next.config.ts` - добавить Supabase Storage в remotePatterns
- [x] `tsconfig.json` - исключить `prisma/` из Next.js компиляции
- [x] `prisma validate` - схема валидна
- [x] `npm run build` - чистый build
- [ ] **ЖДЕТ ПОЛЬЗОВАТЕЛЯ:** создать проект на Supabase -> `.env.local` -> `npm run db:migrate` -> `npm run db:seed`

**Критерий «сделано»:** `npm run db:seed` без ошибок, данные видны в `npm run db:studio`.

---

### Фаза 3 - API Routes (объекты) `[x]` → `c669f87` "feat(phase-3): API routes"
**Цель:** REST API для каталога

**Задачи:**
- [x] `GET /api/objects` — список с фильтрами (type, category, area_min-max, price_min-max, metro)
- [x] `GET /api/objects/[id]` — детальная карточка
- [x] `POST /api/leads` — создать заявку
- [x] Валидация входных данных (zod)

**Критерий «сделано»:** все 3 роута возвращают корректные данные. ✓

---

### Фаза 4 - Каталог (фронтенд) `[x]` → `906df2f` "feat(phase-4): catalog page"
**Цель:** страница `/catalog` с поиском и фильтрами

**Задачи:**
- [x] `src/app/catalog/page.tsx` — серверный компонент, SSR-фильтрация через searchParams
- [x] Компонент `ObjectCard` (фото, тип, площадь, цена, адрес)
- [x] Компонент `FilterPanel` (тип сделки, категория, площадь от/до, цена от/до)
- [x] URL-синхронизация фильтров (`?type=RENT&category=OFFICE`)
- [x] Пагинация (12 объектов на страницу)
- [x] Пустое состояние (нет результатов)
- [x] Loading skeleton

**Критерий «сделано»:** фильтрация работает, URL обновляется, SSR. ✓

---

### Фаза 5 - Страница объекта `[x]` → `7be0eed` "feat(phase-5): object detail page"
**Цель:** `/catalog/[id]` — детальная карточка

**Задачи:**
- [x] Галерея фото (lightbox)
- [x] Все характеристики объекта
- [x] Форма заявки по конкретному объекту (POST /api/leads с object_id)
- [x] OpenGraph метатеги (для шаринга)

**Критерий «сделано»:** страница рендерится, форма сохраняет заявку в БД. ✓

---

### Фаза 6 - Загрузка фото `[x]` → `028a6f6` "feat(phase-6+9): photo upload"
**Цель:** фото объектов хранятся в Supabase Storage

**Задачи:**
- [x] `POST /api/upload` — загрузка файла, возвращает URL
- [x] Ограничение: только jpg/png, до 10 МБ
- [x] Оптимизация: `next/image` с Supabase домейном в `next.config.ts`
- [ ] **ЖДЕТ ПОЛЬЗОВАТЕЛЯ:** создать bucket `objects` (public) в Supabase Storage

**Критерий «сделано»:** фото загружается через API, отображается через `next/image`. ✓ (bucket pending)

---

### Фаза 7 - Admin панель (мини-CRM) `[x]` → `f8398d4` "feat(phase-7): admin panel"
**Цель:** `/admin` — управление объектами и заявками

**Задачи:**
- [x] NextAuth credentials auth
- [x] Защита роута `/admin` через middleware
- [x] `/admin/objects` — таблица объектов, кнопки Edit/Archive
- [x] `/admin/objects/new` — форма создания объекта с загрузкой фото
- [x] `/admin/objects/[id]/edit` — форма редактирования
- [x] `/admin/leads` — таблица заявок с фильтром по статусу
- [x] Смена статуса заявки (NEW -> IN_WORK -> DONE)
- [x] Поле «заметки» к заявке

**Критерий «сделано»:** можно добавить объект через UI, заявка меняет статус. ✓

---

### Фаза 8 - Главная + страницы `[x]` → `20b4fe1` "feat(phase-8): homepage"
**Цель:** полный сайт с главной, услугами, контактами

**Задачи:**
- [x] Главная: Hero + Featured объекты (из БД, featured=true) + Услуги + CTA + Контакты
- [x] Услуги: управляются через таблицу `services` в БД
- [x] Юридические страницы: `/privacy`, `/terms` (статичные)
- [ ] SEO: `generateMetadata` на каждой странице (частично)
- [ ] Карта (Яндекс.Карты или статичная картинка) в контактах

**Критерий «сделано»:** все страницы рендерятся. ✓

---

### Фаза 9 - Дизайн и полировка `[x]` → `028a6f6` "feat(phase-6+9): loading skeletons, 404, error boundary"
**Цель:** профессиональный вид, адаптив, детали

**Задачи:**
- [x] Typography: Cormorant/DM Sans
- [x] Цветовая схема: голубой (#0e2a5e) + салатовый (#7cb342)
- [x] Loading skeleton
- [x] 404 страница
- [x] Error boundary
- [x] Favicon: брендовый icon.tsx (К на тёмно-синем)
- [x] OG-изображение: opengraph-image.tsx (edge runtime, 1200×630)
- [x] Homepage metadata: title + description + OpenGraph
- [x] Адаптив: media queries для 900px и 600px — Footer, Directions, FeaturedObjects, About, Contacts, Hero, каталог

---

### Фаза 10 - Deploy `[x]`
**Цель:** продакшн на Render

> Платформа: **Render** (cosmacapital.onrender.com). Два сервиса: `cosmacapital` (Node) + `cosmacapital-db` (PostgreSQL 18), оба в регионе frankfurt.
> Деплой триггерится автоматически при пуше в `origin/master` (GitHub -> Render CD).

**Задачи:**
- [x] `cosmacapital` Web Service на Render - Deployed
- [x] `cosmacapital-db` PostgreSQL 18 на Render - Available
- [x] 32 объекта мигрированы в БД
- [x] Middleware auth для /admin
- [ ] Supabase Storage: создать bucket `objects` (public) для фото объектов
- [ ] Кастомный домен cosmacapital.ru -> Render Settings -> Custom Domain
- [ ] Smoke test: каталог, заявка, /admin/leads

**Критерий «сделано»:** сайт живет на cosmacapital.ru, заявки приходят в /admin/leads.

---

## Итог (заполнить после завершения)

### Реализовано
- [x] Фазы 1-9 реализованы и задеплоены на Render (cosmacapital.onrender.com)
- [x] 32 объекта мигрированы в БД
- [x] Admin панель с NextAuth, CRUD объектов, CRM заявок
- [x] Каталог с SSR-фильтрами, поиском, пагинацией
- [x] Страницы объектов с галереей и формой заявки
- [x] Upload API для фото (Supabase Storage)
- [x] Главная с Featured объектами и Услугами из БД

### Коммиты по фазам
- Фаза 1 → `6b4ebbb` "feat: add full site skeleton"
- Фаза 2 → `4e8f326` "feat(phase-2): Prisma schema (Object/Lead/Service), seed data, next.config image domains"
- Фаза 3 → `c669f87` "feat(phase-3): API routes (GET /objects, GET /objects/[id], POST /leads), Prisma v5"
- Фаза 4 → `906df2f` "feat(phase-4): catalog page with SSR filters, ObjectCard, FilterPanel, Pagination"
- Фаза 5 → `7be0eed` "feat(phase-5): object detail page with photo gallery, specs, lead form, OG meta"
- Фаза 6+9 → `028a6f6` "feat(phase-6+9): photo upload to Supabase Storage, loading skeletons, 404, error boundary"
- Фаза 7 → `f8398d4` "feat(phase-7): admin panel (NextAuth, objects CRUD, leads CRM with status/notes)"
- Фаза 8 → `20b4fe1` "feat(phase-8): homepage with featured objects from DB, services from DB, unified LeadForm"
- Фаза 10 → `c97f6f2` "docs: update plan - fix deploy platform Railway -> Render (actual)"

---

## Открытые вопросы

- [x] Telegram-уведомления о новых заявках — реализованы в `src/lib/notify.ts`. Активируются добавлением `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` в Render Environment.
- [x] Цветовая схема — голубой #0e2a5e + салатовый #7cb342.
- [x] Экспорт заявок — CSV через `/api/admin/leads/export`, кнопка в /admin/leads.

## Что осталось пользователю (требует credentials)

- Задать `ADMIN_EMAIL` и `ADMIN_PASSWORD` в Render Dashboard (убраны из render.yaml по соображениям безопасности)
- Добавить `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` для уведомлений
- Supabase Storage bucket (инструкция: `scripts/setup-domain.md`, скрипт: `scripts/setup-supabase-storage.js`)
- Домен `cosmacapital.ru` — DNS + Render Custom Domain
